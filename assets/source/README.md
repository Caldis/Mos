# Source Assets

This directory stores editable or historical source material for project
assets. Files here are kept for maintenance and re-export work; they are not
automatically included in the macOS app target.

Import app runtime images into `Mos/Assets.xcassets` before referencing them
from Swift or storyboard files.
