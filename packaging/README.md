# Packaging

Distribution packaging assets and helper commands live here.

- `dmg/`: DMG layout assets and the local `create-dmg` wrapper.

Keep active packaging inputs next to the packaging script that consumes them.
General README media and app source artwork live in `assets/`; release feed
sources live in `release/`. Build artifacts live in ignored directories such
as `build/` and `website/out/`.
