# Always resolve against the public registry. A lockfile regenerated while a
# machine points at a private mirror bakes that mirror into every `resolved`
# URL, and GitHub Pages deploys then fail in `npm ci` (the runner can't reach
# it) — that broke every deploy from 2026-06-16 to 2026-08-20.
registry=https://registry.npmjs.org/
