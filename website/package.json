{
  "name": "mos-home",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build && APPCAST_TMP=.appcast.xml.tmp && (test -f ../build/appcast.xml && cp ../build/appcast.xml $APPCAST_TMP || test -f ../docs/appcast.xml && cp ../docs/appcast.xml $APPCAST_TMP || true) && rm -rf ../docs/* && mv out/* ../docs && cp CNAME ../docs/ && touch ../docs/.nojekyll && (test -f $APPCAST_TMP && mv $APPCAST_TMP ../docs/appcast.xml || true)",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "@next/third-parties": "^15.1.6",
    "next": "15.5.2",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "swr": "^2.3.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "15.1.5",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "typescript": "^5"
  }
}
